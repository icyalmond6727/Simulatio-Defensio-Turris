name: Build Tower Defense Releases

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build Windows EXE
      run: pyinstaller --windowed --name "TowerDefense_Windows" --add-data "assets;assets" main.py

    - name: Upload Windows Folder
      uses: actions/upload-artifact@v4
      with:
        name: TowerDefense-Windows
        path: dist/TowerDefense_Windows/

  build-macos:
    runs-on: macos-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build macOS APP
      run: pyinstaller --windowed --name "TowerDefense_Mac" --add-data "assets:assets" main.py

    - name: Upload macOS Folder
      uses: actions/upload-artifact@v4
      with:
        name: TowerDefense-Mac
        path: dist/TowerDefense_Mac.app